# Include once in /etc/nginx/nginx.conf inside the http { } block:
#   include /path/to/yohto-cleaning-service/deploy/nginx/00-upstream-upgrade.conf;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
